const express = require("express");
const router = express.Router();
const fetch = require('node-fetch');


// GET "/indeed/search" => renders indeed hbs search in home
router.get("/jobs", async (req, res, next) => {

    

})























module.exports = router;